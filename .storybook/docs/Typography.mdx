import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Docs/Tokens/Tipografia" />

# Tipografia

Três famílias declaradas em `@theme`:

- `--font-sans` → **Inter** (UI default)
- `--font-serif` → **Instrument Serif** (display)
- `--font-mono` → **JetBrains Mono** (code)

<div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
  <div>
    <div style={{ fontFamily: "var(--font-sans)", fontSize: 32 }}>Sans — Inter</div>
    <code style={{ fontSize: 12, color: "var(--color-ink-3)" }}>font-family: var(--font-sans)</code>
  </div>
  <div>
    <div style={{ fontFamily: "var(--font-serif)", fontSize: 36 }}>Serif — Instrument Serif</div>
    <code style={{ fontSize: 12, color: "var(--color-ink-3)" }}>font-family: var(--font-serif)</code>
  </div>
  <div>
    <div style={{ fontFamily: "var(--font-mono)", fontSize: 22 }}>Mono — JetBrains Mono</div>
    <code style={{ fontSize: 12, color: "var(--color-ink-3)" }}>font-family: var(--font-mono)</code>
  </div>
</div>
